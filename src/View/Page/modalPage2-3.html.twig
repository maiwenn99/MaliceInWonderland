{% extends 'layout.html.twig' %}

{% block stylesheet %}
    <link rel="stylesheet" href="/assets/css/page/modal.css"

{% endblock %}

{% block content %}
    {% include '_navbar.html.twig' %}
    <div id="frame">
        <div id="border">
            {% for key, placewitheggs in placeswitheggs if key ==2.4%}
                <a href="../Page/page24"><img src="{{ placewitheggs.image }}"></a>
                {% for egggold in eggsgold if placewitheggs.id == eggold.id %}
                    {{ placewitheggs.id }}{{ eggold.id }}
                {% endfor %}
            {% endfor %}

        </div>
    </div>
{% endblock %}